import { routePaths } from "./routePaths";

export const appRoutes = [
  routePaths.home,
  routePaths.about,
  routePaths.contact,
  routePaths.login,
  routePaths.signup,
];

export default appRoutes;
